[package]
name = "ferrous_redis"
version = "0.1.0"
edition = "2021"
description = "A Redis-compatible in-memory data store, built from scratch in Rust."

[[bin]]
name = "ferrous-redis"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = 3
lto = true
